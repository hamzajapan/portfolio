import React, { useState } from 'react';

// Import the existing Resume component content
import ResumeContent from '../components/Resume';

export default function Resume() {
  return <ResumeContent />;
}